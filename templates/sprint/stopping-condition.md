# Stopping Condition

The goal of this sprint must be **machine-checkable**: a single command that
exits 0 when the sprint is done and nonzero otherwise. Live QA runs this
command verbatim and the exit code is the verdict — no judgment calls.

Replace the placeholder below with your real command. It runs from the
repository root via `python3 scripts/verify_stopping_condition.py --sprint <id>`.

```bash
echo replace-me-with-a-real-command
```

> **Note:** if you cannot write the command, you do not yet know what you
> are building. Stop and define the goal before planning.
